# Frontend — sticker-search

Next.js app. Ver [README da raiz](../README.md).

```bash
pnpm install
pnpm dev
```

App em http://localhost:3000 — consome `GET /api/v1/stickers` e `GET /api/v1/search`.
